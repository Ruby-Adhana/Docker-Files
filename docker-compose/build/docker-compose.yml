services:
  web:
    build: .
    image: myflaskapp
    container_name: flask-app-container
    ports:
      - "5000:5000"
    depends_on:
      - redis

  redis:
    image: redis:alpine
    container_name: redis-container
